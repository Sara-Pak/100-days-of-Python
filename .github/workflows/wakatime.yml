name: Waka Readme

on:
  workflow_dispatch:
  schedule:
    # Runs at every 12AM UTC
    - cron: "0 0 * * *"

jobs:
  update-readme:
    name: Update this repo's README
    runs-on: ubuntu-latest
    steps:
      - uses: 100-days-of-Python/waka-readme@master
        with:
          WAKATIME_API_KEY: ${{ secrets.waka_76302c8c-48a3-4e60-9ac2-ddd6b203daea }}
          GH_TOKEN: ${{ secrets.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 }}
          SHOW_TITLE: true
          TIME_RANGE: all_time
          BLOCKS: ❤
          SHOW_TOTAL: true
